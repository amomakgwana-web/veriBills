import { Card, EmptyState } from "@veribills/ui-kit";

export function EventBusMonitor() {
  return (
    <Card>
      <EmptyState
        title="Event Bus Monitor"
        hint="Recent events, delivery status, and failed deliveries across xBilling and xUtilities land here in the next build stage."
      />
    </Card>
  );
}
